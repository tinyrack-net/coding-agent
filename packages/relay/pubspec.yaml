name: tinyrack_relay
description: Paseo-compatible relay E2EE primitives and encrypted channels.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  pinenacl: ^0.6.0

executables:
  tinyrack-relay: tinyrack_relay

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
