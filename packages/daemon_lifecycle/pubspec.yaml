name: daemon_lifecycle
description: Shared daemon lifecycle logic (pid lock, liveness probe, spawn/stop) used by the daemon and the desktop app.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  agent_protocol:
    path: ../protocol
  path: ^1.9.0
  web_socket_channel: ^3.0.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
